import React from 'react';
import Layout from './Layout';

function Main() {
    return (
        <Layout>
            {/* Inertia handles routing automatically */}
        </Layout>
    );
}

export default Main;
